language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
#  - "3.4"
#  Python 3.4 build/test fails due to mock.patch not working.
#  Does not fail due to code failing tests
  - "3.5"
  - "3.6"
cache: pip
install:
  - pip install -r requirements.txt
script:
  - unit2 discover --pattern=test_*.py